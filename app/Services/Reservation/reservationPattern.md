# Reservation — resource booking + calendar sync

## `Main::historyReservations()` — SQL-level hard cap, not true pagination

The user-panel history tabs (previous/cancelled/released/upcoming) fold recurring-series occurrences
into one representative row (`groupBy(fn($r) => $r->parent_id ?? $r->id)`) with an accurate
`series_count` — needing every occurrence of a series in PHP to count correctly, so the query can't
`->limit($historyLimit)` at the SQL level the way `resources()`/`totalResources()` do. Before the fix
this meant `$query->get()` had **no SQL limit at all**: `scopeCancelled()`/`scopePrevious()`/
`scopeReleased()` carry no time window, so a long-tenured user's ENTIRE lifetime history in that status
was fetched, hydrated (with `resource` eager-loaded), and grouped/mapped in PHP on every page load of
the tab — only to discard everything past `$historyLimit` (5, +5 per "load more") via `->take()`.
Confirmed via direct code read, not a log symptom.

**Fix applied is a quick safety cap, not a true fix**: `private const HISTORY_QUERY_CAP = 500;`
applied as `$query->limit(self::HISTORY_QUERY_CAP)->get()` before the PHP-side grouping. This bounds
the worst case (truly unbounded growth) but does **not** make the tab genuinely paginated — every load
still fetches up to 500 rows regardless of `$historyLimit`. **Known, accepted tradeoff**: if a single
recurring series has more occurrences than fit before the 500-row cutoff (e.g. 500+ historical rows in
one status where the cutoff lands mid-series), that series' `series_count` badge could undercount — a
cosmetic edge case, not a functional bug, only reachable well past what any UI session realistically
pages through. A real fix would push the series-grouping and counting into SQL (e.g. a
`COALESCE(parent_id, id)` group-by + window function) so only `$historyLimit` rows are ever fetched —
deliberately not done; flagged as a future architecture change if this tab's cost ever becomes real
rather than theoretical. Regression test:
`ReservationTest::test_history_query_applies_hard_ceiling_before_series_folding` (asserts the actual
executed SQL contains `limit 500` via `DB::listen()`, not just that the component renders).

## `EventSyncService` — meeting bookings become one shared calendar Event, not two mirrored ones

`Reservation::booted()` calls `EventSyncService::sync()` on every `saved` and `EventSyncService::purge()`
on `deleted`, regardless of whether the reservation was created via the admin `ReservationResource` or
the user-panel `Dashboard\Reservation\Main` Livewire component — one model hook, both entry points, no
per-entry-point duplication.

### Shape

For an **active** reservation on a **Meeting**-type resource, `sync()`:
- `Event::updateOrCreate`s exactly **one** `Event`, owned by the booker (`user_id = $booker->id`),
  keyed on `(user_id, description)` where `description` embeds the reservation id
  (`"جلسه برنامه‌ریزی شده از طریق سیستم رزرواسیون #{id}"` — the stable identity key across repeat syncs).
  `private = true`. Title is party-agnostic (`"جلسه {booker} و {related}"`) since both viewers see the
  same row.
- `EventShare::firstOrCreate`s a share for the resource's `relatedUser` (`Resource::relatedUser()`,
  matched by `name`) — this rides the **already-existing** `App\Services\Menu` shared-events
  infrastructure (`app/Services/Menu/statePattern.md`) instead of building new notification plumbing:
  the invited colleague gets the `shared-events:nudge` bell immediately (`SharedEventsNudge`,
  `date >= now`) and the `shared-events` badge dot within 24h of the meeting
  (`EventShare::hasImminentFor`).
- Both writes go through **Eloquent** (`updateOrCreate`/`firstOrCreate`/model `->delete()`), never a
  bulk query, because `HasMenuState`'s flush and `NudgeServiceProvider`'s triggers are bound to Eloquent
  model events — a bulk `Event::query()->delete()` or `EventShare::insert()` here would silently break
  the badge/nudge wiring.

### Superseded design

Before this, `sync()` created **two** independent private `Event` rows (one per `user_id`, booker and
related) — an unlinked mirror, not a share. That meant the invited person got a silent calendar entry
with **zero** notification (no `EventShare` row, so `SharedEventsNudge`/`SharedEvents` never fired), and
each party could edit/delete their own copy independently. The new shape trades that independent-edit
capability for the existing, already-shipped nudge+badge visibility — the invited person's calendar
entry is a **shared, read-only** view (`Calendar.php`'s `editEvent()`/`deleteEvent()` are ownership-gated
to `user_id = Auth::id()`, so only the booker can edit/cancel; the related person sees it via the same
`orWhereHas('shares', ...)` clause any other shared event uses).

### `pruneOtherOwners()` — legacy-row cleanup, not resource-dependent

Both `sync()` and `purge()` delete any `Event` row matching this reservation's `description` but **not**
owned by the booker (`pruneOtherOwners()`), independent of whether `Resource::relatedUser()` currently
resolves. This both consolidates any pre-existing row left over from the old two-Event scheme (the next
time that reservation is saved or cancelled) and stays correct if the resource is later renamed/deleted
such that the name-match breaks — `purge()` no longer needs to load `resource.relatedUser` at all.

### Timing — nudge vs. badge are decoupled, by the existing system's own design

This decoupling is existing, unmodified behavior from `app/Services/Menu/statePattern.md` §
`SharedEvents` — the reservation flow simply now participates in it rather than introducing new
notification timing.

### Not yet backfilled

Reservations that were active before this change may still carry a leftover related-user-owned `Event`
row from the old scheme. It self-heals the next time that reservation is saved or cancelled
(`pruneOtherOwners()` runs on both paths) — no migration/backfill command has been run against
production data for this.

### Calendar UI — reservation-linked events are read-only, not just read-only-by-convention

`EventSyncService::isReservationEvent(?string $description)` / `::reservationIdFrom(?string $description)`
are the single source of truth for "was this `Event` produced by a reservation" (checked against the
`description` prefix, `DESCRIPTION_PREFIX`) — `Calendar.php` uses them rather than re-deriving the marker
string. Without this guard the booker could open the normal edit/share modal on a reservation-generated
`Event` and change its title/date/shares directly in the UI; those changes would either get silently
clobbered on the next `sync()` (title/date/the one canonical share) or drift permanently out of sync with
the reservation (extra manual shares — `sync()` prunes any share not belonging to the resource's current
`relatedUser`).

`Calendar::editEvent()`, `::deleteEvent()`, and `::openShareModal()` each early-return with an error toast
for a reservation-linked event instead of performing the action, even though the booker is technically the
`Event` owner. `events.blade.php` mirrors this: the normal share/edit/delete button row is swapped for a
single "مشاهده رزرو" (view reservation) link — `route('reservation')`, and a day-list tag distinguishes
these rows from an ordinary shared event. The calendar legend documents this row type alongside the
existing private/public/ownership rows.

**Not** `route('dashboard', ['tab' => 'reservation', ...])`: unlike `feed`/`gallery`/`post`/etc., `reservation`
is not one of `Tabs::tabs()`'s keys — it is registered in `TabPresenter::shortcuts()` as its own dedicated
route (`routes/web.php`: `Route::get('/reservation', Reservation::class)->name('reservation')`), so a
`dashboard?tab=reservation` URL silently falls back to the `home` tab via `Tabs::normalizeTab()`. The
`?open={id}` `FocusOnRecord` deep-link convention doesn't carry over either: `Reservation\Main::focusRecord()`
resolves `open` against `Resource::find($id)` (for highlighting a bookable resource from search), not
`Reservation::find($id)` — there is no UI wiring today to scroll to/highlight one specific past reservation
by id. `selectedDayEvents()` still exposes `reservation_id` per event for a future such deep-link; until
that's built, the link only routes to the reservation module's landing page.

## Long-hold (range) booking mode — one record, continuous span

A continuous hold of a resource (e.g. "reserve this seat for a whole year") is stored as **one** `Reservation` row with `start_time` today and `end_time` months/years ahead. No new column, no migration — `end_time` is already a `timestamp`. Range mode is **elevated-only (admin or developer) by construction**: only the Filament admin form (`ReservationFormPresenter`) — reachable by both `admin` and `developer` roles (`User::canAccessPanel` returns `true` for both; `developer` is super-by-role and bypasses `AuthorizesByPermission`) — exposes a free `end_time`; the user panel derives `end` from `selectedDuration` (minutes, bounded by `max_duration_minutes`), so a regular user can never produce a multi-day span.

### Deriving `isRange` — duration, not calendar-span

`BookingContext::isRange()` and `Reservation::isRange()` both compute `!is_full_day && start->diffInDays(end) >= 1`. This is a **>=24h elapsed** check, deliberately not a calendar-day (`isSameDay`) check: a legitimate short overnight slot (22:00→01:00, ~3h, crosses midnight) returns `diffInDays ≈ 0.08` → **not** range → stays under the normal intraday validators (`Duration` max/min, `AllowedHours`). Only holds ≥ ~1 day become range. A year is 365 days → range.

### Validators relaxed vs. kept in range mode

In range mode these **early-return** (a continuous hold isn't bound to working hours/days or intraday caps): `Duration`, `AllowedHours`, `ResourceSchedule`, `AllowedDays`, and the `window_days`/`window_hours` future-cap inside `TimeWindow`. `TimeWindow`'s **past-start guard is kept** even in range (no backdated starts).

These **stay enforced** in range: `ResourceAvailability` (the per-resource double-book guard — the seat is exclusively held for the whole span; nobody else can book it), `TypeActive`, `ResourceActive`, `FullDay`, `Recurrence`, `UserActive`, `BookingPermission`. `UserConflict` is **skipped** in range: it is per-(user, resource-type) and would lock the user out of *every* same-type booking for the year; `ResourceAvailability` already prevents true per-resource double-booking, so the per-type lockout serves no purpose for an elevated-assigned (admin or developer) hold.

`ActiveLimit` (`max_per_user`) counts a range **once**, in its start month — elevated-authoritative (admin or developer) ranges are exempt from the monthly cap for later months (documented, accepted).

### Form guards (`ReservationFormPresenter::isRangeState`)

When the form's `start_time`/`end_time` form a range (`!is_full_day && diffInDays >= 1`): the `is_full_day` toggle is `disabled()` (with `->dehydrated()` so `false` persists) — you can't flip a range to full-day; `is_recurring` + `recur_pattern` + `recur_count` are `disabled()` and hidden — you can't combine range + recurring (which would otherwise make `GenerateSeriesAction` shift the year-long `end` per child into overlapping year-long holds). `end_time` and `start_time` pickers carry `->maxDate(now()->addYears(10))` to reject absurd spans.

### Page hooks

`CreateReservation::afterCreate` guards `GenerateSeriesAction` with `!$this->record->isRange()`. `EditReservation::mutateFormDataBeforeSave` forces `is_full_day = false` for a range and only applies the same-day `startOfDay/endOfDay` pin when **not** range — so editing a range never truncates it to a single day.

### Event sync — no meeting event for a range

`EventSyncService::sync` routes a range through the same `purge()` branch as a non-meeting/non-active reservation: no `Event::updateOrCreate` (a seat/desk hold is not a calendar meeting), and the `DB::afterCommit` `countdown:active`/`StateService::flush` still runs. The countdown is `Event::activeCountdownEvent()`-driven, not reservation-`end_time`-driven, so a far `end_time` never produces a "525600 minutes remaining" artifact.

### Cancel / partial release — one record, whole-only by design

A range hold is one record. **Whole-cancel** (status → cancelled) and **shorten** (move `end_time` earlier) both work in one step. **Release** (`releaseAction`) on an **ongoing** range (start in the past) truncates `end_time` to now and sets status `released` — the remainder of the span is freed for others; on a **future** range (not yet started) it sets status `released` only (does *not* free — use cancel to free a not-yet-started hold). A mid-range **single day/time-slot cannot be excluded** from within the one record — there is no per-day exception mechanism. To free a mid-span gap: truncate `end_time` to the gap start, then create a second reservation for the remainder (two records). This is the accepted tradeoff vs. the 365-discrete-records approach (which silently truncated on `window_days`/`max_per_user`/non-working-days and lied about coverage). If real per-day blackouts inside a span are later required, a child `reservation_exceptions` table is the documented extension point — not built.

### Rendering

`Reservation::displayTime` shows the end **date** when end is on a later day than start (`date • H:i تا end_date H:i`), covering the user-panel history card (`history.blade.php` uses `$reservation->display_time`). The admin list table gained an `endTime()` column (`toJalaliSmart`, toggleable, hidden by default).

### Calendar rendering of long holds — boundary markers, not a per-day pill (2026-09-05)

Left unhandled, a range reservation of any length repeats its `event_seat` pill/bar on **every** day/week it spans (§"Calendar private reservation injection" in `app/Livewire/livewirePattern.md`) — fine for a few days, pure noise for a months-long hold. `Reservation::isLongHold()` (`spanInDays() >= self::LONG_HOLD_DAYS` — const, `7`; since 2026-09-15 both `isRange()`/`isLongHold()` delegate to the private `spanInDays()` helper instead of each computing its own `diffInDays`, semantics unchanged) marks the cutoff; below it, rendering is byte-identical to any other range reservation (no regression).

For a long hold, `CalendarPresenter`:
- **`userReservationsByDay()`** stops fanning the reservation into every day key in its span — it emits the reservation into `byDay` only on its **start** day-key and/or its **end** day-key, and only when that boundary actually falls inside the currently-rendered `[start, end]` window. Both the month-grid dot/count and the day/week in-grid pill inherit this for free — they all read this one method.
- **`userReservationsSpanning()`** (the week-view cross-column bar) skips long holds entirely — a bar that redraws itself fresh every single week for months would be its own kind of noise.
- **`reservationPill()`** takes an optional `$dayKey` and, for a long hold, appends `· شروع` (start) or `· پایان` (end) to the title when that day IS the boundary being rendered — so the one or two pills that do appear read as a start/end marker, not a mystery one-off booking. The **start** pill only also carries `long_hold_days` (int, `start->diffInDays(end)`) and `long_hold_end_date` (Jalali short date, e.g. `۰۴ بهمن`); both `null` on every other pill, including the end boundary.
- **`buildMonthDays()`** derives a per-day `longHoldDays` (nullable int) straight from the same `userReservationsByDay()` result it already fetches for `hasReservations`/`eventCount` — zero extra query. `month.blade.php`'s day cell renders it as a tiny sage number next to the `event_seat` dot, only on the reservation's start day.
- **`calendar/events.blade.php`** (the per-day event list — the one place that lists everything happening that day in full) reads `long_hold_end_date` off the reservation entry and renders a dedicated badge — `event_seat` icon + "از طریق رزرو" + "- تا {end date}" — placed **before** the private/public badge whenever the day's entry is reservation-linked, on its own row (`flex-wrap`, so a long resource name never gets clipped).

No standalone "ongoing holds" strip exists in the header — one was built (a persistent `long-holds.blade.php` partial + a separate `longHolds()` query, included in `view-header.blade.php` above the grid) and removed the same day: the month-grid badge and the events-list badge together cover "how long" and "until when" without an always-visible extra chrome element or a second query. If a persistent cross-navigation surface is wanted again later, re-derive it from `buildMonthDays()`'s data rather than a separate query. Tests: `CalendarTest` (`test_long_hold_*`, `test_selected_day_events_shows_long_hold_duration_only_on_its_start_day`).

The 7-day threshold and the sidebar/grid badges reuse the existing sage/`--tool-sage-*`/`--md-sys-color-tertiary*` token convention already used by `reservation-banner.blade.php` and `events.blade.php`'s own reservation badge, so the new UI reads native, not bolted on. Calendar's own legend (`calendar/legend.blade.php`, rules tab, "از طریق رزرو" row) documents this for end users.

## Admin form date handling — shared `PersianDateFieldService` + `FilamentDateHandler`

The reservation admin form is now consistent with the other 6 Filament modules (Task, Ths, Event, Gallery, Profile, Report): date/datetime fields use `PersianDateFieldService` (Jalali year/month/day dropdowns → hidden `Y-m-d`) + a native `type('time')` TextInput, merged by the `FilamentDateHandler` trait — **no raw `DatePicker`/`DateTimePicker`**. `ReservationFormPresenter` was the last holdout.

- `start_time`/`end_time` are split in the form as `{field}_date` (`PersianDateFieldService::make`, `fullWidth: true`, `yearTo: now+10y`) + `{field}_time` (TextInput, default 09:00/17:00). `end_time_date`/`end_time_time` are `visible` only when `!is_full_day`; `start_time_date` is always visible (full-day needs the start date; end is implicit `endOfDay`).
- `ReservationResource\Pages\CreateReservation` and `EditReservation` `use FilamentDateHandler` with `datetimeFields() = [start_time, end_time]` (`default_time '00:00'`).
- **Lifecycle gotcha (do not regress):** the merge + validation + isFull-day/range normalization all happen inside `mutateFormDataBeforeCreate`/`mutateFormDataBeforeSave` (which receive the `$data` that is actually persisted via `handleRecordCreation`/`handleRecordUpdate`). There is **no `beforeCreate`/`beforeSave`** — those hooks read `$this->data` which, for Create, is NOT the same array as the persisted `$data`, so validating there would see un-merged split fields (`start_time` null) and diverge from what is saved. Keep the merge+validate+normalize in the `mutateFormDataBefore*` method.
- Carbon `startOfDay()`/`endOfDay()` mutate in place — in the isFull-day branch use `$start->startOfDay()` then `$end = $start->copy()->endOfDay()` (copy before endOfDay) so `validateEdit` receives `$start=00:00:00`, not the mutated `23:59:59`.
- `isRangeState(Get)` (form guard) assembles from the raw `start_time_date_year/_month/_day` and `end_time_date_year/_month/_day` parts via `PersianDateFieldService::assemble()`, not `start_time`/`end_time` (those only exist post-merge) nor the bare `start_time_date`/`end_time_date` hidden fields (those only carry the assembled value at dehydration/validation time, since `PersianDateFieldService`'s `->live()` production-timeout fix — see `filamentPattern.md` — moved assembly off the per-pick interaction path).
- Range `maxDate(now+10y)` became `PersianDateFieldService`'s `yearTo: Jalalian::now()->getYear() + 10` (the service's native cap, not a chained `->maxDate`).
- Strings: `fields.start_time_time` / `fields.end_time_time` added to `lang/fa/resources/reservation/strings.php`.
- **Gotcha (caught 2026-09-23, resolved the ERP-native way):** this migration renamed the presenter's date-field methods, but two `RelationManager`s reusing `ReservationFormPresenter` (`ResourceResource\RelationManagers\ReservationsRelationManager` and `UserResource\RelationManagers\ReservationsRelationManager`) were never updated — they kept calling the old `fullDayDate()`/`startTime()`/`endTime()` names, which no longer exist, making their edit/create actions a guaranteed PHP fatal error. A `RelationManager` in another resource's directory tree is easy to miss during a presenter refactor since it isn't exercised by the main `ReservationResource` form or its own tests. Final fix (same day): both RMs declare `$relatedResource = ReservationResource::class` and their `form()`/`infolist()` overrides are deleted — the Edit table action becomes a URL link to the real `EditReservation` page (where `FilamentDateHandler` runs; no CreateAction exists on either RM), the ViewAction stays a modal rendering `ReservationResource`'s own form/infolist via vendor delegation. Both RM tables carry `->toolbarActions([])` so the delegation does not inherit the main table's bulk delete/exporter (user decision 2026-09-24); their redundant `getModelLabel()`/`getPluralModelLabel()` overrides are deleted (delegation supplies identical labels). No RM-level schema duplication remains to drift. When renaming/removing any `*FormPresenter` static method, grep for the method name across `Pages/`, `RelationManagers/`, and `Schemas/` in every resource, not just the owning one.

## Range is elevated-only (admin or developer) — server-side guard in `BookAction`

Range mode is elevated-only (admin or developer) **by construction** (only the Filament admin form exposes a free `end_time`; the user panel derives `end` from `selectedDuration` bounded by `max_duration_minutes`). As defense-in-depth against a misconfigured `max_duration_minutes >= 1440` (which would let a regular user pick a 24h+ duration and create a range), `BookAction::execute` (user-panel entry point, called only from `Reservation\Main::book()`) rejects up front using `hasElevatedRole()` (admin **or** developer — developers are not blocked):

```php
if (!$user->hasElevatedRole() && !$isFullDay && $start->diffInDays($end) >= 1) {
    throw new \Exception('رزرو بلندمدت فقط از طریق پنل ادمین قابل ثبت است.');
}
```

`Main::book()` catches the exception and toasts the message (no 500). This guard lives in `BookAction`, NOT in the shared `ValidationService` pipeline, because `validateBooking` runs with `enforceAllRules=true` on elevated create too — a shared `skip_admin` rule would still run for elevated users (admin or developer) on create (the `skip_admin` optimization only applies to `validateEdit`) and wrongly reject elevated ranges. The user-panel-only `BookAction` is the correct place. Test: `RangeBookingTest::test_book_action_rejects_range_for_non_admin`.

## Range length cap — `max_range_days` policy + `RangeDuration` validator

The intraday `Duration` (`max_duration_minutes`) is skipped in range mode (a continuous hold isn't an 8h meeting), so without a separate cap a 20-day hold sails through. The `max_range_days` policy (per resource type, in `reservation_policies`, configurable in `ReservationPolicyResource` form next to `max_per_user`) closes that gap: the `RangeDuration` validator (registered in `ValidationService::$bookingRules` with `skip_admin => false`, so admin create **and** edit are both bound) rejects a range whose `start->diffInDays(end) > max_range_days` with `ReservationError::RangeTooLong` (ERR-025, "حداکثر مدت رزرو بلندمدت N روز است"). Unset/`null` ⇒ no cap. Only applies in range mode (`isRange()` early-return for ≤1-day). This also bounds the 24h threshold discontinuity from the range side. Tests: `RangeBookingTest::test_range_duration_*` (blocks exceeding, allows within, no-cap-when-unset, ignored-for-non-range).

## Avantgarde edge-case fixes (2026-09-15)

Four gaps found by an adversarial edge-case sweep of the avantgarde view, all fixed minimally:

- **`focusRecord()` bypassed permission/kill-switch gating.** Unlike `switchTab()`, the search-deep-link (`?open={id}`) handler reassigned `$this->activeTab` to the resource's type with no `canBookTab()` check — a stale/shared link into a locked or unpermitted type rendered a fully bookable-looking UI the server would reject at submit. Fixed with the same guard `switchTab()` already uses: `if (! $resource || ! $this->canBookTab($resource->type)) return;`.
- **Avantgarde was missing the recurring-booking toggle entirely.** `booking-panel.blade.php` never included `recurring.blade.php` — `$isRecurring`/`recurPattern`/`recurCount`/`recurPreview()`/`allowsRepeat()` were all already live on `Main` and rendered in classic, but avantgarde users had no way to create a recurring series without switching views. Fixed with one `@include('livewire.dashboard.reservation.recurring')` after the time-rail include, before the hints chip row — `recurring.blade.php` is a fully self-contained partial (own `wire:key="recurring-panel"` root), zero new PHP.
- **Command Grid's "maximize" state survived switching to the Booking Canvas**, hiding header/nav/footer with no visible way to un-toggle (Esc still worked, just wasn't discoverable). Fixed with one attribute on the canvas tab-selector's wrapping div in `avantgarde.blade.php`: `x-on:click="if (max) toggleMaximize()"` — resets `max` (from the shared `maximizeMixin()` on the root `x-data="reservation()"`) on any click in that area, including the canvas-tab buttons themselves.
- **A focused/open resource could silently swap to a different one** once it dropped out of the currently-rendered `resources()` window (booked out, deactivated, or `resourcesLimit` reset back to 6 by a date/floor/time change) — `deckReconcileOpen()` client-side only re-fronts `open` while it's still in `deckOrder`; once gone, whatever id is now first renders as the "open" face with no click. Fixed server-side in `invalidateResourceCache()`: after the existing unset/refetch, `if ($this->open && ! $this->resources->contains('id', $this->open)) $this->clearFocus();` — guarded on `$this->open` being set, so the common (nothing focused) path pays nothing extra; the `resources()` re-evaluation this forces was already going to happen at render time regardless (Livewire `#[Computed]` caches it), so no added query.

Regression tests: `ReservationTest::test_focus_record_ignores_locked_type`, `::test_focus_record_ignores_unpermitted_type`, `::test_open_resource_clears_when_it_drops_from_filtered_list`, `::test_avantgarde_booking_panel_includes_recurring_toggle`. The maximize-reset fix is pure client-side Alpine state with no server round-trip to assert against — not covered by this suite (PHPUnit/Livewire testing has no DOM/JS execution here).

**Six more minor polish fixes (2026-09-15), same sweep:** `updatedGridPinned()` now resets `$this->gridLimit = self::GRID_PER_PAGE` (a pin toggle re-sorts the grid, so staying on a stale load-more position could strand a row); `booking-panel.blade.php`'s type-selector renders `<x-ui.empty icon="lock" .../>` instead of a blank dropdown when `$tabs` is empty (zero booking permission); `book()`/`cancel()` each gained a `catch (\Illuminate\Database\Eloquent\ModelNotFoundException $e)` before the generic `\Exception` catch, dispatching a fixed Persian toast instead of leaking the model's English message; `dossier.blade.php` gained a `sr-only focus:not-sr-only` skip link to `#dossier-card-{id}` (shown only when a resource is open and more than one resource is listed); `command-grid.blade.php`'s action-icon cluster got a `.reservation-action-cluster` class with a `.app-density-compact` override in `dashboard.css` (mirrors the file's existing `.reservation-command-table tbody td` compact rule); a pin-count hint appears above the grid when `gridPinned` exceeds the 50-row cap `historyGroups()` already enforces. The cross-tab `$store.pinned`/`$store.tagged` localStorage race (two open tabs can clobber each other's pin/color choices) was left unfixed — it needs a `storage`-event listener in `resources/js/**`, out of scope while those files are being hand-edited elsewhere.

**Cancel-quota bar surfaced (2026-09-15).** `cancelLimitUsage` (`count`/`max`/`blocked`) was already computed every render to power `bookingBlockReason()`'s block message, but was never shown to the user otherwise — `reservation.blade.php` gained a second progress bar ("لغوهای این ماه: N / M", `wire:key="reservation-cancel-usage"`) directly below the pre-existing active-quota bar, same markup/style, error-colored when `blocked`. Zero new query — pure reuse of an already-computed value. Shared by both view modes, same as the active bar.

## Dynamic metadata facet filtering — zero-maintenance, model-owned discovery (2026-09-15)

Superseded an earlier, hand-built floor+unit filter (two hardcoded `#[Computed]` methods, two hardcoded blade blocks, gated to `activeTab === 'seat'||'spot'`) with a fully generic mechanism that requires **zero code changes** when an admin adds a new resource type or a new `metadata` field to an existing one. Design reviewed by Fable 5 before implementation; two thresholds below were explicit user sign-off, not arbitrary picks.

- **`Resource::metadataFacets(string $type): array`** (in `app/Models/Concerns/HasResourceMetadata.php`, beside the pre-existing `FIELD_CONFIG`) is the single source of truth. It pulls every active resource's `metadata` for the type, and for every scalar top-level key (plus every scalar entry inside the `custom` KeyValue bag, unwrapped to `custom->{key}`) computes presence-ratio and distinct-value-count. A key becomes a facet only when **presence ≥ `FACET_MIN_PRESENCE` (0.4)** and **2 ≤ distinct values ≤ `FACET_MAX_OPTIONS` (15)**. Array-valued keys (`available_days`, `time_slots`) are skipped automatically — they fail `is_scalar()`, no denylist entry needed; `notes` is the one entry in `FACET_SKIP` (free text, never facet-worthy). The whole thing is `ModelCacheVersion::remember(static::class, "facets:{$type}", ...)` — Tier V, auto-invalidated by the project-wide `ModelCacheServiceProvider` listener on any `Resource` save/delete, no manual cache wiring.
- **Why 0.4/15, not "any field, any cardinality":** a field only 2/50 resources have filled in would make a filter button that narrows to almost nothing — noise, not a filter. A field with 30+ distinct values (e.g. `card`/`extension`, near-unique identifiers) would render 30 chips, an unusable UI — it degrades gracefully to search-only instead (see below) once a type's fleet crosses the cap. Both numbers are consts next to `FIELD_CONFIG`, changeable in one place, not per-key code.
- **Floor keeps its bespoke labels** ("طبقه منفی N" for negative, "همکف" for zero) via `facetValueLabel()`, a `match()` keyed on `$key === 'floor'` inside the SAME model file — the user's constraint was "no meta-conditionals in Blade," not "no config anywhere"; a future bespoke label for a new key is one more `match` arm here, still zero Blade changes.
- **`Main.php`**: `$filterFloor`/`$filterUnit`/`availableFloors()`/`availableUnits()`/`setFloor()`/`setUnit()` deleted entirely (not layered — floor/unit re-emerge from discovery automatically given the real data shape). Replaced by one `array $facetFilters = []` (key→value), one `#[Computed] facets(): array` delegating to `Resource::metadataFacets($this->activeTab)`, one `setFacet(string $key, string $value)` (same toggle-clears-on-reselect idiom `setFloor`/`setUnit` used), and `applyFacets($query)` — resolves each `facetFilters` entry against the CURRENT `$this->facets`' `key => path` map before building a `where("metadata->{$path}", $value)`, so a stale/tampered filter key that isn't a real discovered facet is silently dropped rather than reaching the query as an arbitrary JSON path.
- **`Resource::scopeAvailable()`** lost its `$floor`/`$unit` params entirely — availability (type/status/day/time-slot/conflict) is the scope's only job now; facet filtering lives in `Main::applyFacets()`, applied on top of `Resource::available(...)`.
- **`filter.blade.php`** is now one generic `@foreach($this->facets as $facet) ... @foreach($facet['options'] as $opt) ...` — zero key- or type-specific `@if`s. Included, unconditionally once reached, inside `<x-ui.forms.filters>` (the same reusable search+dropdown-filter component already used by Command Grid/Dms/Ths) in BOTH `reservation.blade.php` (classic) and `booking-panel.blade.php` (avantgarde — the first time either a filter or a resource search has ever existed there), each wrapped in an `@if(count($this->facets) > 0)` with its own static `wire:key` (`resv-facets-panel-classic` / `resv-facets-panel-booking`) per the morph-safety rule — a type with no discoverable facets shows no filter panel at all, not an empty one.
- **Resources became searchable as a side effect of the reuse**: `resourceSearch` (bound via `<x-ui.forms.filters>`'s `searchModel`) matches both `name` and, as the high-cardinality-identifier fallback, `metadata` itself — so `extension`/`card` values that fall out of the facet-chip UI past the 15-option cap stay findable by typing them. Uses `INSTR(name, ?) > 0` / `INSTR(CAST(metadata AS CHAR), ?) > 0` (plus the `json_encode`-escaped needle variant for Persian `metadata` content), not `LIKE`, per `livewirePattern.md`'s `getBaseQuery()` free-text-search rule — same two bugs (`%`/`_` over-matching, `\uXXXX`-escaped Persian under-matching on the JSON-cast column) apply here as documented there.
- **Tests**: `tests/Feature/Models/ResourceModelTest.php` covers `metadataFacets()` directly — discovers floor+unit, excludes notes/array fields/low-presence/single-value keys, unwraps `custom`, empty-type → `[]`, floor's special labels. `tests/Feature/Livewire/Dashboard/ReservationTest.php` covers the Livewire-level wiring — `setFacet()` toggle, reset clears `facetFilters`, a facet genuinely narrows `resources()`, `resourceSearch` narrows by name, `focusRecord()`/open-drop-out both reset/react to `facetFilters` (not the deleted floor/unit properties). The two `metadataFacets()` fixture tests use a synthetic `zz-seat-facet-test` type rather than the real `seat` type — the dev DB already carries ~90 active `seat` resources with real `floor` values, which polluted both the distinct-option-count assertion and the single-value-exclusion assertion when run against real `seat`/`spot` types (coreTestPattern.md §6's baseline-delta rule). `facetValueLabel()`'s negative-floor branch (`'طبقه منفی ' . abs($n)`) was concatenating a raw ASCII int instead of running it through `convertToPersian()` like every other digit in this file — fixed to `convertToPersian(abs($n)) ?? abs($n)`, matching the digit-conversion convention used everywhere else in `HasResourceMetadata`.

## View modes — `classic` (default) / `avantgarde` (Booking Canvas + Command Grid)

`Main::$view` mirrors `Tab\Gallery`'s `$view` toggle pattern exactly: `mount()` reads `session('reservation_view_mode', 'classic')` whitelisted to `['classic', 'avantgarde']`; `toggleView(string $view)` re-whitelists, assigns, and writes the same session key. `$view` is deliberately absent from `resetFilters()`'s and `switchTab()`'s unset/reset lists — the chosen mode is a cross-cutting view preference (like Gallery's), not per-tab/per-filter state, so it survives both. `reservation.blade.php` branches the whole tab-selector→date→time→filter→recurring→divider→cards+history block on `$view` (`reservation-view-classic` / `reservation-view-avantgarde` `wire:key`s, the classic branch byte-identical to before this feature) — the monthly-quota usage bar above the branch is shared by both modes and stays outside the `@if`.

**`avantgarde.blade.php` itself is now a thin shell** — a top-level `<x-ui.buttons.tab-selector>` bound to a real Livewire property, `public string $canvasTab = 'booking'` (whitelisted `['booking', 'grid']` via a dedicated branch prepended to `switchTab()`, checked before the history-tab and resource-type-tab branches — a genuine server round-trip on every canvas switch, not a client-only toggle), then an `@if($canvasTab === 'booking') ... @else ... @endif` including one of two sibling partials in `reservation/`: `booking-panel.blade.php` (left, 50%, `lg:grid-cols-2`) + `dossier.blade.php` (right, 50%) for booking, or `command-grid.blade.php` alone for grid. `reservation-canvas-booking` / `reservation-canvas-grid` are the branch's `wire:key`s.

**All heavy PHP moved to `AvantgardePresenter`, per the project's own presenter convention (`viewPattern.md` §3b — static config never lives in a blade `@php` block).** `app/Livewire/Dashboard/Reservation/Presentation/AvantgardePresenter.php` has six static methods — `canvasTabs()`, `weekDayLabels()`, `activeTypeMeta(array $tabs, string $activeTab)`, `calendarCells(int $year, int $month, array $availableDates, string $selectedDate, ?array $allowedDays = null)`, `blockedDayLabels()`, `timeRail(array $slots, string $startTime, string $endTime, array $busySegments, ?string $selectedDate = null)` — all threaded through `Main::render()`'s view-data array (only computed when `$this->view === 'avantgarde'`, so classic pays nothing for any of it) and shared transitively into every `@include`d partial via Blade's normal scope-sharing. **Why this split exists — a real compiler bug, not a style preference:** the first version of `avantgarde.blade.php` (one ~400-line file, all logic inline in `@php` blocks) tripped the same `Livewire\Features\SupportCompiledWireKeys\ExtendBlade` compiler bug already documented at the top of `livewirePattern.md` and in `reminderPattern.md` §3a — "sensitive to the file's total compiled complexity, not a specific shape." Confirmed via `app('blade.compiler')->compileString()` + `php -l` bisection (the same technique those two docs prescribe): the file's own `@if`/`@endif`/`@foreach` nesting was correctly balanced, but Livewire's morph-marker instrumentation still produced a `ParseError: unexpected token "else"`. The fix — extracting `booking-panel.blade.php` and `time-rail.blade.php` as their own `@include`s, and moving the two large `@php` blocks (calendar-cell generation, time-slot/tick-mark math) into the presenter — is the exact same remedy those docs already prescribe: don't hunt for "the" bad line, reduce total compiled complexity per file. **If a future edit to any of `avantgarde.blade.php`/`booking-panel.blade.php`/`time-rail.blade.php`/`dossier.blade.php` reintroduces a `ParseError: unexpected token "else"/"endif"`, re-run the same compileString+php-l bisection before assuming a smaller shape is safe — don't re-inline the split as a "cleanup."**

**`booking-panel.blade.php`** — the command column: a resource-type selector (a solid-button-opens-dropdown widget, per `viewPattern.md` §8.2's "Solid-button dropdown for filters" shape — same `x-data="{ open: false }"`/`@click.outside`/transition classes as `<x-ui.month-filter>`, but showing the *currently active* type's icon+label on the trigger since a type is always selected, unlike month-filter's nullable "all months" state; options `wire:click="switchTab({{ $tab['id'] }})"`, reusing the exact `$tabs` array `ResourceType::tabs()` already produces — never a second type-enumeration mechanism), an inline month calendar (real 7-column grid, `AvantgardePresenter::calendarCells()`-driven, `setDate()`/`nextMonth()`/`prevMonth()`/`goToday()` — all pre-existing methods, no new PHP state). **Day-cell semantics (2026-09-15):** each cell carries a `reason` from the presenter (`past`/`day_off`/`window`, null when available) — `day_off` days render struck-through, past/window days muted, and every unavailable day shows its reason via a native `title` tooltip from `blockedDayLabels()`; the header has a «امروز» quick-jump next to the month chevrons.

**Visual restyle to match `tab/calendar/month.blade.php` (2026-09-15):** the grid was rebuilt to reuse that file's card-family grammar instead of its own bespoke one — `aspect-[1/0.85]` cells (was fixed `h-11`), `rounded-[14px]` (was `rounded-xl`), a `ring-1` + depth `shadow-[...color-mix...]` per state instead of flat backgrounds, and the weekday header gained the bottom border + `tracking-wider` treatment. State mapping onto Reservation's actual data (`tab/calendar/month.blade.php` has no "unavailable day" concept, Reservation does): selected → primary fill + glow shadow + ring (no scale pop, unchanged); today (not selected) → primary-container + ring (upgraded from a bare ring-only outline); available/normal → surface + subtle outline-variant ring + hover; `day_off` → muted surface-container-low + faint ring, strikethrough kept; other unavailable reasons → even more muted, no ring. Every `wire:click`/`disabled`/`title` binding and all presenter data (`$cell['selected']`/`available`/`isToday`/`reason`/`day`/`value`) is untouched — pure class-level restyle, zero PHP/JS change., and (via `@include('...time-rail')` when `! $isFullDayTab`) the drag-rail time picker. `booking-panel.blade.php` also renders the rules/hints chip row from the pre-existing `minNoticeHours`/`durationBounds`/`selectedDuration`/`bookingBlockReason` computeds.

**`time-rail.blade.php`** — a two-handle selection rail snapping only to `AvantgardePresenter::timeRail()`'s `slots` array (never an arbitrary continuous value), shaded by `openResourceBusySegments()` (see below), with an hour-tick scale row (`timeRail['ticks']`, five landmark slots, each absolutely positioned at its own `right: X%` so labels sit exactly on their slot, not at flex-spread positions) under the track. **RTL-native (2026-09-15): all rail geometry is `right: X%`-anchored so time flows right→left** — earliest slot at the right edge, matching the tick row and the start/end summary chips under `dir="rtl"` (the earlier `left:`-anchored version contradicted both). Interaction: dragging a handle commits via `$wire.setStartTime()`/`setEndTime()` on pointer release; clicking anywhere on the track jumps the *nearest* handle; arrow keys nudge a focused handle ±1 slot (30 min) **and commit immediately** — no release needed. Busy segments render as a neutral hatch (`repeating-linear-gradient` over `on-surface-variant`), not an error tint — occupied ≠ error. A client-side `conflict()` (overlap iff `startIdx < busyEnd && endIdx > busyStart`, from the presenter's `busyIdx` pairs) tints the selection error-colored and swaps the summary's live Persian-duration chip for «تداخل با رزرو فعلی» — a visual warning only; server-side validation at submit remains the sole gate. A `nowIdx` hairline marker renders when the selected date is today. A `fa(s)` JS helper (not a PHP call — `convertToPersian()` is server-only) renders the live-dragged labels in Persian digits; every sibling data property is referenced via `this.` inside its methods (the org-chart `openTools` gotcha in `viewPattern.md` §8.13 — bare identifiers silently write to `window`). **Extraction (2026-09-15):** the whole `x-data` body — previously a ~63-line inline object literal — now lives in `resources/js/components/alpine/data/reservationTimeRail.js`, registered `Alpine.data('reservationTimeRail', ...)` via the module's existing lazy-load registration (`resources/js/components/alpine/modules/reservation.js`, alongside the sibling `reservation` factory). The blade's `x-data` is now a single call — `x-data="reservationTimeRail(@js($timeRail['slots']), @js($timeRail['busyIdx']), {{ $timeRail['nowIdx'] ?? 'null' }}, {{ $timeRail['startIdx'] }}, {{ $timeRail['endIdx'] }})"` — same 5 constructor args, same order, byte-identical method bodies (the one deliberate change: `fa()`'s Persian-digit array is now a module-level constant instead of rebuilt per call). The wrapping `<div>`'s dynamic `wire:key="reservation-avantgarde-time-rail-{{ $this->activeTab }}-{{ $this->open ?? 'none' }}-{{ $this->date }}"` — unchanged — is what makes taking constructor args safe at all: it forces Alpine to fully reinitialize with fresh values whenever those three things change, so the named-factory call re-inits exactly like the inline literal did. See `app/Livewire/livewirePattern.md`'s client-side-follow-up section for the sibling `selectedDuration`/`startSlotMeta` moves this continues. **Visual alignment with the booking-panel calendar (2026-09-15):** the start/end drag-handles gained a `ring-1 ring-[color-mix(in_srgb,var(--md-sys-color-primary)_50%,transparent)]` plus a depth `shadow-[0_8px_20px_color-mix(...)]` (was a flat `shadow-md`) — `rounded-full` stays correct for the handles themselves (they're knobs, not cards — see `viewPattern.md`'s icon-container anti-pattern note, which explicitly carves out toggle knobs as legitimate `rounded-full` uses). The start/end/duration summary chips moved from `rounded-lg` to `rounded-[14px]` plus a matching `ring-1`, mirroring the booking-panel calendar's day-cell radius and ring language documented above — same reasoning: reuse one card-family grammar across the avantgarde command column instead of each widget inventing its own. Zero functional change — every `:class`/`x-text`/`x-on` binding is untouched, only static/`:class` literal strings.

**`dossier.blade.php`** — the resource stage. Was originally an absolutely-positioned z-index fan (a literal "stack of folders"), which forced a fixed `min-h` and either cramped or scrolled once more than a few resources were in the active tab. **Layout revision (2026-09-15, live-tested):** now a plain CSS grid (`grid grid-cols-2 sm:grid-cols-3 gap-3`) — the open resource (`deckDepth(id) === 0`) gets `col-span-full` (via a `:class` binding, Tailwind JIT-safe) and its full face treatment (image/plate, hero-stat, metadata, notes disclosure, CTA, wrapped in `<span @click.stop>` so its "ثبت رزرو" submit click doesn't also bubble into the card root's `deckOpen()`); every other resource is a compact tile (icon + name) that wraps naturally into as many rows as needed, all simultaneously visible with zero scrolling and zero fixed height. `deck.js`'s per-resource `deckStyle()` transform/z-index method was deleted as dead weight once the fan visual was removed — `:class="deckDepth(id) === 0 ? 'col-span-full' : ''"` is the only per-resource style decision now, computed straight from the id-keyed `deckOrder` array (no index-based logic). **Float-to-front removed (2026-09-15):** `resources()`'s `->when($this->open, orderByRaw('CASE WHEN id = ? THEN 0 ELSE 1 END'))` was deleted — it reordered the query on every open, so each prev/next cycle morphed the whole grid and other cards visibly jumped to the top for a moment. DOM order is now stable across opens/cycles; the open card simply renders `col-span-full` in its natural grid position (tiles above and below it stay put), and `deckOrder` rotation happens purely client-side in `deck.js` (`deckSync` preserves known ids' existing order across morphs). The one gap this leaves — a server-side `open` (search deep-link `?open={id}` or command palette, which never goes through `deck.js`) — is covered by `reservation.js`'s `deckReconcileOpen()`, called after every `deckSyncFromDom()` (init + the morph hook): if `$wire.open` is set but isn't `deckOrder[0]` yet, it runs `deckBringToFront(open)`. Opening a folder reuses `FocusOnRecord::$open` (the `data-rf="resource-{id}"` scroll/flash marker keeps working unmodified — it scrolls to the card wherever it sits), but **not** via a bare `$wire.set('open', id)` — see the "global focus-chip collision" fix below (2026-09-15) for why a dedicated `focusDeck(int $id)` method exists instead. `clearFocus()` (overridden on `Main`, see below) clears it on Esc, and (since Command Grid added a table maximize state to the same root scope) Esc un-maximizes first if a table is maximized, only falling through to `clearFocus()` otherwise (`x-on:keydown.escape.window="if (max) { toggleMaximize() } else { $wire.clearFocus() }"`).

**Bug: the global "نتیجهٔ جستجو" (search result) header chip appeared on every dossier card click (2026-09-15).** `components/dashboard/header/focus-chip.blade.php` (included by ~15 modules, `reservation.blade.php` among them) renders on the bare condition `!empty($open)` — it has no way to know the property was set by a card click rather than a cross-module search deep-link (`?open={id}`, `FocusOnRecord::mountFocusOnRecord()` → `Main::focusRecord()`). Fixed by adding a plain (non-`#[Url]`) companion flag, `Main::$deckOnly`, set only by a new `focusDeck(int $id): void { $this->open = $id; $this->deckOnly = true; }` — `deck.js`'s `deckOpen()`/`deckCycleAndOpen()` now call `$wire.focusDeck(id)` instead of writing `open` directly. `clearFocus()` is overridden on `Main` (trait method aliasing, since a trait can't be reached with `parent::`: `use FocusOnRecord { FocusOnRecord::clearFocus as protected baseClearFocus; }`) to reset `$deckOnly` alongside the trait's original body. `reservation.blade.php`'s chip include became `@include('...focus-chip', ['open' => $deckOnly ? null : $open])` — suppresses the chip for deck-origin opens while still showing it correctly for a genuine search deep-link (which never touches `$deckOnly`, so it stays `false`). The shared `focus-chip.blade.php` itself is untouched; every other consumer's bare, param-less include is unaffected. See `traitPattern.md`'s `FocusOnRecord` entry for the general rule this establishes for any future module tempted to repurpose `$open` for a non-search local UI concern. **Image treatment (2026-09-15):** the open resource's plate carries a hover-revealed zoom button wired to the SAME `$wire.zoomImageUrl` property + `<x-ui.modals.base wire:model="zoomImageUrl">` lightbox the classic cards use (`cards.blade.php` owns its own instance inside the classic branch; `dossier.blade.php` owns a byte-identical second instance inside the grid branch — one per view mode, never shared across the view-mode `@if`, so the modal morphs with its own branch) — the button needs `x-on:click.stop` in the dossier only, because the card root's `deckOpen()` click handler is absent on classic cards. The image overlay is deliberately NOT a gradient: the resource name sits on its own opaque chip (`bg-black/40` + `border-white/10` + white text, the classic card's exact plate chip) so it reads over any image without tinting the image itself. **Details (2026-09-15, caption-band revision):** the details — hero stat + every `formatted_metadata` item — render as ONE inline row in a solid `h-9` caption band at the BOTTOM of the plate (`shrink-0 ... overflow-x-auto custom-scrollbar` over `surface-container` with a top hairline; bare text + primary-colored icon per item, no chip boxes — an overlay-on-photo draft and a body-row draft were both rejected, the second because it changed the approved card height). The plate became `flex flex-col` — image area `flex-1 min-h-0` (zoom button, name chip `bg-black/40`, and the plate arrows all live inside it) with the band as a sibling below, so the card's TOTAL height is byte-identical to the approved state; only the image loses the band's 36px. The band is `@if`-guarded (`$hero !== null || count($resource->formatted_metadata) > 0` — the accessor returns a plain array, so `count()`, never `->isNotEmpty()`) so image-only resources keep a full-bleed plate. The card body stays notes disclosure + submit CTA. **Desktop height coupling — tried across many rounds, ultimately abandoned (2026-09-15).** The open card was made to stretch and grow proportionate to the booking (calendar) column's height instead of staying content-short: a full-height cut, a bare-⅔ cut, a +100px cut, and a doubled cut were each rejected in turn; ⅔+30% (86.666%) was accepted, then bumped twice more on follow-up requests to 92% and `lg:basis-[97%]`; when a meeting-type resource (tall stage, from the time-rail beneath the calendar) stretched its portrait photo to a visibly distorted ~950×800px, a `lg:max-h-[520px]` cap was added to bound the photo — which only pushed the problem sideways into a large blank gap at the bottom of the card. At that point the mechanism itself — coupling the dossier's height to a sibling column via `items-start` removal + `basis-[]` + `self-stretch` + `h-full` — was judged the actual defect, not any specific percentage: the calendar column's height varies by resource type (meeting has the time-rail, seat/spot/car don't) and by content, so no fixed fraction or cap could stay proportionate across all of them. **Reverted entirely, live-screenshot-confirmed regression → simpler fix, not another tuned number:** the outer two-column grid (`avantgarde.blade.php`, `reservation-canvas-booking`) has `items-start` back on it (columns no longer stretch to match each other); the dossier column wrapper dropped `lg:flex lg:flex-col`; the stage grid (`dossier.blade.php`) dropped `lg:grow-0 lg:basis-[]  lg:grid-rows-1`, back to a plain `grid grid-cols-2 sm:grid-cols-3 gap-3 items-start`; the card dropped `lg:self-stretch`, its open face dropped `lg:h-full`; the plate is now one plain fixed height, `h-56` on mobile and `lg:h-80` on desktop — no `flex-1`/`min-h`/`max-h` chain at all. A card now sizes purely to its own content on every viewport, structurally immune to whatever height the calendar column happens to render at. If cross-column visual balance is wanted again later, don't retry percentage/stretch coupling — it was tried five times across two sessions and failed each time for the same structural reason. Prev/next cycling reuses the shared `<x-ui.buttons.rail-arrows>` with its opt-in `position="plate"` variant (added for this consumer, documented in `viewPattern.md` §8.12; an earlier draft hand-rolled plate-flank chevrons and was replaced, per §7's mandatory-adoption rule): `prev="deckCycleAndOpen(-1)"` / `next="deckCycleAndOpen(1)"` / both `*-show="deckOrder.length > 1"`, hosted INSIDE the plate div — safe now because the plate variant is `inset-0` + vertically centered with NO outward push, so the plate's `overflow-hidden` (mandatory: it clips the image to the rounded top corners) cannot clip the buttons the way the default `-top-5` variant would (the §8.12 gotcha an earlier outside-hosting draft hit). The variant's row is `pointer-events-none` with `pointer-events-auto` buttons, and the consumer wraps the component in `<div class="contents" x-on:click.stop>` — note that this wrapper is an ancestor of the row's whole `inset-0` area, so it stops EVERY click originating on the plate, not just the arrow buttons; that is harmless here only because the open card's plate click is a no-op re-open (`deckOpen` on the already-open id). Without the stop, button clicks bubble to the card root's `deckOpen(id)`, which re-opens the card just cycled away from and silently cancels the cycle (the component's own `@click` handlers can't carry `.stop` without forking it). Plate arrows are visible at ALL viewports (unlike the default `hidden md:flex` row) since there is no tile-tap alternative for the open face. Keyboard parity: the card root binds `arrow-left` = next (`deckCycleAndOpen(1)` — deckOrder follows DOM order, +1 is visually leftward) / `arrow-right` = previous, same leftward=forward convention as the time-rail; but keydowns only fire when the card HAS focus, so the root also carries `x-effect="if ($wire.open == {id}) $el.focus({ preventScroll: true })"` — opening (click, deep-link, or a cycle) auto-focuses the open card, making continuous arrow navigation work without tabbing.

**Bug: cycling next/prev stranded the open card mid-grid, visually sitting between the tiles and the load-more button (2026-09-15).** `deckCycleAndOpen()` is client-only at the instant it runs: `deckCycle()` mutates the Alpine `deckOrder` array synchronously, so every card's `:class="deckDepth(id) === 0 ? 'col-span-full' : ''"` binding flips IMMEDIATELY — but the underlying DOM order is server-controlled (`resources()`'s `->when($this->open, orderByRaw(...))` float-to-front, applied only once the `$wire.focusDeck(id)` round-trip resolves and Livewire morphs). For the round-trip's duration, the newly-open card was `col-span-full` while still sitting in whatever grid cell its OLD DOM position put it in — CSS Grid auto-placement then wrapped it into a mid-grid full-width band, with tiles before it staying above and tiles after it (plus the load-more button) trailing below, exactly the "dossier sits between chips and load more" symptom. Not a `wire:key` issue — keys only govern morph identity, not layout order. **Fix:** decouple visual position from DOM position entirely via CSS `order`, reactive off the same client state that already drives `col-span-full`: `:style="` + "`order: ${deckDepth({{ $resource->id }})}`" + `"` on the card root. Since `deckDepth()` returns 0 for whichever id is currently front-of-deck, the open card always renders first regardless of its DOM slot, and every other tile's `order` follows its own `deckOrder` position too — so the WHOLE grid's visual sequence tracks the client-side cycle state instantly, with the (harmless, invisible) DOM reorder catching up silently once the round-trip lands.

**Open/close face-swap animation — tried twice, removed entirely (2026-09-15).** First attempt: a slide+fade (`translate-x-4`→`0` on enter, reverse+further on leave) on both `x-show` faces, meant to read as continuous leftward motion — reported as "two animations fighting" because `col-span-full`/`order:` (the grid-position fix above) are discrete, non-animatable grid properties, so the outer card box snaps to its new size/position in a single frame while the inner content tried to slide inside a box that had already jumped. Second attempt: dropped the translate, opacity-only fade — still reported jittery (the box-snap plus a genuinely simultaneous property-transition on the card root, see below, was still visually inconsistent even without directional motion). **Final: no transition at all** — both `x-show` divs are bare (`x-show="..." x-cloak`, no `x-transition:*`), and the card root's `transition-all duration-300 ease-out` was ALSO removed (it was animating border/shadow/background on the same element whose `col-span-full`/`order:` were snapping instantly, compounding the inconsistency). Open, close, and cycle are now instant with zero motion — explicit ERP-grade choice over decorative animation, not a placeholder pending a future fix. If animation is wanted here again, know going in: any transition on this card's content or root will fight the non-animatable `col-span-full`/`order:` grid-position change unless the grid-position change itself is also animated via the FLIP transform technique (measure-then-transform, not a CSS transition) — a plain `x-transition`/`transition-*` here has now failed twice for the same structural reason.

The z-stack's id-ordering machinery (`deckOrder`, `deckSync`/`deckSyncFromDom`, `deckDepth`, `deckBringToFront`, `deckCycle`, `deckOpen`/`deckCycleAndOpen`) in `resources/js/components/alpine/mixins/deck.js` is unchanged and still load-bearing — it decides which ONE resource renders as the big face vs. a small tile, independent of DOM/query order — spread into the module's root `x-data="reservation()"` factory alongside a Gallery-style `view` mirror and a `Livewire.hook('morph', ...)` DOM re-sync, matching `Tab\Gallery`'s own morph-hook convention.

**`openResourceBusySegments` — supplemental visual only, never relaxes availability.** The one new computed this feature adds: `Reservation::where('resource_id', $this->open)->whereDate('start_time', $this->date)->whereIn('status', [Active, Released])->get(['start_time','end_time'])`, guarded to return `[]` immediately when `$this->open` is null — zero extra query on every render except the one where a resource is actually opened on a timed (non-full-day) tab. It only shades the avantgarde time-rail with the opened resource's existing bookings for the selected date; it is read-only presentation and never touches `Resource::available()`'s own conflict scope, which remains the sole source of truth for whether a slot can actually be booked. Registered in `invalidateResourceCache()`'s unset list (alongside `resources`/`totalResources`/`selectedDuration`/etc.) since it depends on both `$this->open` and `$this->date`, both of which flow through `setDate()`/`setFacet()`/`setStartTime()`/`setEndTime()`/`resetFilters()` — all of which already call `invalidateResourceCache()`.

**wire:key map (avantgarde.blade.php / booking-panel.blade.php / dossier.blade.php):**
- Mode branch roots (`reservation.blade.php`): `reservation-view-classic` / `reservation-view-avantgarde`, both `class="contents"` (multi-root branches).
- Canvas branch root (`avantgarde.blade.php`): `reservation-canvas-tabs-{{ $canvasTab }}` on the tab-selector instance itself; `reservation-canvas-booking` / `reservation-canvas-grid` on the two `@if`/`@else` branches (the grid branch is a `class="contents"` wrapper).
- Calendar day cells: `avantgarde-cal-day-{{ $day }}` — keyed by the plain Jalali day-of-month (1–31), not a date string or DB id, so navigating between months morphs the same 31 cells in place instead of tearing down/rebuilding the grid; leading blank cells are `avantgarde-cal-pad-{{ $i }}` (loop index, non-conditional decorative padding).
- Time rail root: `reservation-avantgarde-time-rail-{{ activeTab }}-{{ open }}-{{ date }}` — deliberately dynamic (unlike the static-key rule for record panes): the rail's `x-data` seeds `slots`/`busyIdx`/`nowIdx`/`startIdx`/`endIdx` once at Alpine mount, and a Livewire morph patches the attribute text but never re-initializes the component — so any input the seeds depend on (resource type with its own `allowed_hours`, opened resource, date) must change the key to force teardown/rebuild with fresh seeds (same technique as `reservation-canvas-tabs-{{ $canvasTab }}`). Per-segment keys `reservation-time-rail-busy-{i}`, tick keys `reservation-time-rail-tick-{i}`; hints row: `reservation-avantgarde-hints` with per-branch keys `reservation-hint-{notice,bounds,duration,block}` (all four hint `@if`s flip on date/time/tab selection); now-marker branch: `reservation-time-rail-now`; calendar controls branch (the `@if($this->dateWindow !== null)` header row, can flip on resource-type change): `avantgarde-cal-controls`.
- Dossier tile loop: `dossier-card-{{ $resource->id }}` per the existing `resources()` collection.
- Empty state (no resources for the active type/date): `reservation-avantgarde-empty`, wrapping `<x-ui.empty>` in a `class="contents"` div per `livewirePattern.md`'s "`<x-ui.empty>` swallows `$attributes`" rule.
- `data-rf="resource-{{ $resource->id }}"` on the tile root (not an inner element) — same convention/consumer as every other `data-rf` site in the app (`FocusOnRecord`'s scroll/flash).
- `data-resource-id="{{ $resource->id }}"` (tile root) is a second, JS-only attribute — `deckSyncFromDom()` scans it after every Livewire morph to keep the client-side `deckOrder` array (front-to-back id order) in sync with whichever resources the server actually returned (tab switch, load-more, floor filter), independent of `data-rf`.

## Command Grid — the second avantgarde section (`command-grid.blade.php`)

A dense table rendering of the same history state the classic sidebar (`history.blade.php`) already pays for — not a second data source: both views read from the shared private `historyGroups()` query (classic via the `historyReservations()` computed's `take(historyLimit)`, grid via the `gridReservations()` computed's `take(gridLimit)`), sharing `activeHistoryTab`/`switchTab()`/`cancel()`. Switching between the Booking Canvas and Command Grid is the real `$canvasTab` server round-trip described above (deliberately NOT a client-only Alpine toggle — see that section for why: the tab-selector's ordinary `wire:click="switchTab(...)"` behavior needed to be extended, not bypassed, so the canvas switch reads and behaves exactly like every other tab in this app).

**Maximize button — lives INSIDE the table header, in the left-most action `<th>`, not in the toolbar row above the table.** The th's content is a `flex items-center justify-end w-full gap-1` div whose DOM order is [date-span filter popover, density toggle, column-visibility popover, maximize button] — under this app's RTL flex convention (`viewPattern.md` §8.5: first child renders right, last renders left) that pins the maximize button to the header's left edge, with `pr-3` supplying the right padding, exactly DMS's (`dms/toolbar.blade.php` in the action `<th>`) and Ths's treatment. **The `اقدام` label text was removed (2026-09-15, by request — "meaningless and crowds it")**; every icon button in the cluster already carries its own `title`/`aria-label`. `<x-ui.forms.date-span-popover :active="$this->dateSpanActive()" :basisOptions="null"/>` also moved here from the toolbar row above the table (2026-09-15, "the filter must sit beside the other icons in header") — the `mb-4` toolbar row above the table now holds only `<x-ui.forms.filters>` (DMS's own dockable filter+search bar, `dms/filters.blade.php`'s exact composition). `toggleMaximize()`/`max` (from `resources/js/components/alpine/mixins/maximize.js`, spread into `data/reservation.js` alongside `deckMixin()`) plus `<x-ui.modals.max-backdrop/>` and a `:class="{ 'max-widget': max }"` wrapper reproduce Ths's/DMS's fullscreen-a-widget mechanism verbatim. `<x-ui.forms.filters>` (not Ths's lighter `<x-ui.forms.search>`, and not a second bespoke tab-selector) drives both search (`searchModel="gridSearch"`) and status filtering — its slot renders a "همه" (all) chip plus one chip per `$historyTabs` entry, `wire:click="showAllHistory"` / `wire:click="switchTab($id)"` respectively, `activeCondition`/`clearAction` overridden from the component's own default `activeFilter`-based props (which this module doesn't use) to `historyShowAll`/`showAllHistory()` instead. The other three header `<th>`s remain plain label-only.

**Density + column visibility (2026-09-15) — pure reuse of the DMS/THS table mechanisms, zero new stores.** The compact toggle is the same global `$store.density` (`density.js`) DMS uses: it toggles the root `app-density-compact` class, and `dashboard.css` gained one mirror rule (`.app-density-compact .reservation-command-table tbody td { @apply !py-0 }`) beside the existing `.dms-doc-table` one — deliberately GLOBAL state (toggling it here also affects the DMS table and the taskboard), same as the DMS toggle. Column visibility extends `$store.colVisibility` (`colVisibility.js`) with a third scope: `DOMAINS`/`KEYS`/`SCOPES` gained `'reservation'` (`reservation-col-hidden` localStorage key, `.reservation-command-table` CSS class), a `reservationHidden` getter for the popover's count badge, and the two hardcoded `for (i < 2)` loops became `for (const d of DOMAINS)` so the CSS-mask render covers all scopes. The table itself carries `class="reservation-command-table"` and `data-col="resource|time|status"` on those three th/td pairs — the اقدام column has NO `data-col` and can never be hidden (it hosts the header controls and the row actions). Both controls + the popover live in the اقدام header toolbar (THS's in-header pattern), DOM order [label, density, columns popover, maximize] so maximize stays left-most. The canvas tab was renamed «فهرست و مدیریت» → «مدیریت» (`AvantgardePresenter::canvasTabs()`), one word like Ths's tabs; the legend's grid note was updated for the new name and now also flags that density/columns are browser-local like the row's palette/pin buttons.

**`historyShowAll` — the real default, not `activeHistoryTab`'s `'upcoming'`.** `public bool $historyShowAll = true;` — Command Grid opens with NO status chip active and every reservation status mixed together, ordered `orderByDesc('start_time')`, guarded (like `gridSearch`) to `$this->view === 'avantgarde' && $this->historyShowAll` so classic's sidebar is never affected regardless of this property's default. `showAllHistory()` sets it true and resets `historyLimit`/unsets both history computeds (a no-op guard if already true); `switchTab()`'s existing history-tab branch now also sets `historyShowAll = false` whenever a specific status chip is picked, and its early-return guard checks `historyShowAll` too (`if ($this->activeHistoryTab === $tab && ! $this->historyShowAll) return;`) — without that second condition, clicking a chip that happens to equal the stale `activeHistoryTab` value while "all" was showing would silently no-op instead of narrowing.

**`history_bucket` — a real per-row derived status, not a blind read of `$activeHistoryTab`.** Because "all" mode mixes every status in one result set, the status badge and the cancel-button's visibility can no longer trust `$activeHistoryTab` (that's a single filter value, not a per-row fact) — every row would otherwise show whatever the last-clicked filter was, regardless of its real status. `AvantgardePresenter::historyBucket(Reservation $r): string` re-derives the same four buckets `Reservation`'s own `scopeUpcoming`/`scopePrevious`/`scopeCancelled`/`scopeReleased` use (`cancelled_user`/`cancelled_admin` status → `cancelled`; `released` status → `released`; `active` status with `end_time < now()` → `previous`; else → `upcoming`) from the row's actual `status`/`end_time`. `historyReservations()`'s existing per-group `->map()` closure (which already attaches `series_count`/`cancel_warning`) now also calls this (only actually re-derives when `$showAll` is true; otherwise it trusts `$activeHistoryTab` directly, since every row in a scoped query already shares that bucket by construction) and attaches `history_bucket`. `command-grid.blade.php`'s status `<td>` and its "لغو رزرو" cancel button both branch on `$reservation->history_bucket`, never `$activeHistoryTab`, for exactly this reason.

**`gridSearch` — a dedicated property, empty-string no-op guard.** `public string $gridSearch = ''` filters `historyReservations()`/`totalHistoryReservations()` via `whereHas('resource', fn($q) => $q->where('name', 'like', "%{$this->gridSearch}%"))`, guarded by `when($this->view === 'avantgarde' && $this->gridSearch !== '', ...)` — the `$this->view === 'avantgarde'` half of that guard is load-bearing, not decorative: without it, a stale non-empty `gridSearch` left over from a prior avantgarde session would silently filter the classic sidebar the moment the user switched back to `$view = 'classic'`, since both views share these two computeds. Caught live and fixed mid-build; `test_grid_search_only_applies_in_avantgarde_view` asserts it directly. `updatedGridSearch()` resets `historyLimit` to 5 and unsets both computeds, mirroring `switchTab()`'s existing history-tab reset.

**Pin — reused the pre-existing `$store.pinned` (`resources/js/components/alpine/stores/pinned.js`), not a new store.** A generic boolean pin store (`isPinned(id, scope)`/`togglePin(id, scope)`/`getPinned(scope)`, `Set`-backed, `localStorage` key per scope) already existed for contact/channel/message/project/activity/menu — this is the "verify before building" lever: `KEYS`/`sets` gained one new entry, `reservation: 'pinned-reservations'`. **Pinned-to-top is a server-side sort, not a DOM move (2026-09-15, by request — the earlier "no reordering" descoping was superseded):** `public array $gridPinned = []` on `Main` holds the pinned reservation ids, synced from the client store at exactly two points — the row's pin button (`$store.pinned.togglePin(...); $wire.set('gridPinned', $store.pinned.getPinned('reservation'))`) and `reservation.js`'s `init()` (restores pins from `localStorage` on hard load/refresh, only firing the round trip when the set is non-empty). `historyGroups()` (the shared private backing both computeds) prepends `orderByRaw('CASE WHEN id IN ({intval'd ids, capped at 50}) THEN 0 ELSE 1 END')` as the PRIMARY sort (before the existing per-tab `orderBy`), guarded by `$this->view === 'avantgarde'` so the classic sidebar that shares the query is never reordered; `updatedGridPinned()` unsets both cached computeds. Pinned rows keep server order among themselves (CASE ties, no per-pin rank stored — same minimal semantics as the channel/contact sidebars' CSS-`order` pins); rows are keyed (`command-grid-row-{id}`), so Livewire morph moves them cleanly instead of corrupting cells. This is deliberately NOT the dossier's removed float-to-front: there the reorder fired on every open as a side effect; here it fires only on an explicit pin toggle. Server-testable: set `gridPinned` and assert row order.

**Color tag — reused `$store.tagged` (`resources/js/components/alpine/stores/tagged.js`) with a new `'reservation'` scope**, same `maps`/`KEYS` extension as `pinned.js` above (`reservation: 'tagged-reservations'`). Row markup (button + palette popover + clear button) is copy-identical to `ths/ticket-table-row.blade.php`'s own tag cluster, just re-scoped. **The row also carries Ths's `tagBg` overlay** (`absolute inset-0 -z-10 pointer-events-none` div with `:style` bound to `$store.tagged.tagBg(id, 'reservation')`, first child of the resource `<td>`, which anchors to the `<tr>`'s `relative isolate`) — without it the palette silently "does not work": the tag is stored but nothing is painted. Known edge: the overlay lives in the resource cell, so hiding the منبع column via the header's column-visibility control also hides the row tint. **Stale-bundle gotcha (2026-09-15):** the `reservation` (and `ths`) scopes in `pinned.js`/`tagged.js` were uncommitted for days while the built bundle predated them — pin/palette clicks no-op'd entirely in the browser (`maps[scope]` undefined) until a rebuild + Empty-Cache-Hard-Reload past the PWA SW's known first-reload staleness; if a store-scoped feature "does not work" live, check `git diff` for unbuilt store changes before suspecting the markup.

**Action-column cluster order — palette, then pin, then reminder, reminder always last.** Matches `ths/ticket-table-row.blade.php`'s own ordering and the general rule in `reminderPattern.md`'s "Ths" paragraph (reminder sits last in any such cluster, not module-specific). Cluster offset from the cell's top edge is `top-0.5` (tighter than Ths's `top-2`, by request — a narrower row doesn't need as much clearance). `<x-dashboard.reminder-trigger :for="$reservation" variant="corner" tooltip-position="top"/>` needed zero new backend — `Reservation` already `use`s `HasReminders` (confirmed in `Reservation.php`, documented in `reminderPattern.md`). **The table's outer wrapper deliberately does NOT carry `overflow-hidden`** (unlike a naive card-with-rounded-corners default) — a `top`-positioned tooltip on a row near the top of the table would otherwise be clipped by that overflow box before it can render above the row; the `<thead>`/`<tbody>` cells already carry their own `first:rounded-tr-2xl`/`last:rounded-tl-2xl` corner classes, so dropping the wrapper's `overflow-hidden` costs nothing visually.

**Row actions (separate from the corner cluster, inline like Ths's "مشاهده" button):** a `visibility` icon button — currently a no-op, since neither `history.blade.php` nor anywhere else in this module has a click-to-view-detail mechanism to hook into (explicitly left as a stub per spec, not a fabricated new modal) — and, only when `$reservation->history_bucket === 'upcoming'` (see below — NOT `$activeHistoryTab`, which is meaningless once "all" mixes every status into one result set), the exact same cancel button as `history.blade.php`'s (`@click="$dispatch('open-confirmation', { title, message, method: 'cancel', params: $reservation->id, type: 'dispatch' })"`, byte-identical payload shape).

**`data-rf="reservation-{{ $reservation->id }}"` is new, forward-scaffolding, currently inert.** No existing `data-rf` prefix for reservations-themselves existed anywhere in the app before this (only `resource-{id}` for the bookable resource, from Pass A) — `FocusOnRecord::focusRecord()` on this module resolves `?open=` against `Resource::find()`, not `Reservation::find()`, so this marker has no live deep-link consumer yet, exactly as this file's own pre-existing note already flagged ("`selectedDayEvents()` still exposes `reservation_id`... until that's built, the link only routes to the reservation module's landing page"). Added anyway for consistency with the app-wide `{type}-{id}` `data-rf` convention and to be ready the day a reservation-specific deep-link is built.

**Empty state reuses the classic sidebar's exact key strings, applied at the correct DOM level for a table; load-more, not a numbered pager (2026-09-15, superseded the earlier numbered pager for consistency with `Dms`/`Ths`).** `history.blade.php`'s `@empty` branch is a `<div wire:key="reservation-history-empty" class="contents">` (list markup); Command Grid's `@empty` branch is a `<tr wire:key="reservation-history-empty">` (table markup) — same key text (deliberately, per instruction: this is the same semantic empty state, not a new one), moved from a `div` wrapper to the `<tr>` itself per `livewirePattern.md`'s table-specific morph rule ("NEVER insert a `<div>` wrapper at `<tbody>`/`<tr>` level — key the `<tr>` itself"). The two `wire:key`s never coexist in the DOM (classic vs. avantgarde are mutually exclusive server-rendered `@if` branches), so the shared key string is safe. Both the classic sidebar and the grid now share the identical load-more shape: `public int $gridLimit = self::GRID_PER_PAGE;` (`GRID_PER_PAGE = 10`), `gridReservations()` is `historyGroups()->take($this->gridLimit)` (byte-mirrors `historyReservations()`'s `->take($this->historyLimit)`, same `historyGroups()` single query source), `loadMoreGrid(): void { $this->gridLimit += self::GRID_PER_PAGE; unset($this->gridReservations); }` (mirrors `loadMoreHistory()`). `command-grid.blade.php` renders `<x-ui.buttons.load-more action="loadMoreGrid" .../>` inside `wire:key="grid-load-more"`, guarded by `@if($this->totalHistoryReservations > count($this->gridReservations))` — identical guard shape to `history.blade.php`'s `reservation-history-load-more`. `updatedGridSearch()`/`showAllHistory()`/`switchTab()`'s history branch/`applyDateSpan()`/`clearDateSpan()` all reset `$gridLimit = self::GRID_PER_PAGE` alongside their existing limit resets, and `updatedGridPinned()` unsets the grid computed too (pins reorder the set, not just individual rows). No `WithPagination` trait, no `LengthAwarePaginator` — a plain `public int $gridLimit` property, deliberately: the trait's URL `?page=` machinery would fight this module's tab/deep-link params for a widget-local pager.

**wire:key map (command-grid.blade.php):**
- `command-grid-row-{{ $reservation->id }}` — each data `<tr>`.
- `reservation-history-empty` (on the `<tr>`, see above) — reused verbatim from `history.blade.php`.
- `grid-load-more` — wrapper around `<x-ui.buttons.load-more action="loadMoreGrid" .../>`, rendered only while more rows remain.

**Zero new queries beyond Pass A's `openResourceBusySegments`.** Command Grid adds a `whereHas('resource', ...)` clause to two ALREADY-QUERIED computeds (`historyReservations()`/`totalHistoryReservations()`), not a new query; `history_bucket` derivation is pure PHP over already-loaded attributes, not a query; the pin/tag stores are pure client-side (no query at all); the maximize toggle is pure Alpine state (no query, no Livewire round trip); `<x-ui.forms.filters>`'s search field is `wire:model.live.debounce.500ms`-bound directly to `gridSearch` (one round trip per debounced keystroke, same cost the old dedicated search box already had).

## Release on a range — truncate ongoing, not future

`releaseAction` is range-aware: an **ongoing** range (`isRange() && start_time->isPast()`) is truncated to `end_time = now()` + `status = released` — the remainder of the span is freed for others (the `start_time->isPast()` guard prevents `end_time < start_time` for a not-yet-started range, which would be an invalid record). A **future** range (start in the future) falls back to the existing `status = released` only — it does *not* free the resource (use cancel for that), matching the existing intraday release semantics (released still blocks unless `allow_overlap_release`). Note the prior guide text claiming release "frees the resource" was inaccurate for intraday (it only frees when `allow_overlap_release` is on) — the statuses + admin-ops guide hints were rectified to match the actual `ResourceAvailability` behavior (`released` blocks unless `allow_overlap_release`). Tests: `ReservationResourceTest::test_release_truncates_ongoing_range_to_free_resource`, `test_release_does_not_truncate_future_range`.
